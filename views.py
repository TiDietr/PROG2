def beataetigung(request):
    if request.method == "POST":

        send mail(

        )
        return render_template(request,'bestaetigung.html', {})
    else render_template (request, 'termin.html',{})